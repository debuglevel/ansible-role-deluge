---
- name: Ensure directories
  ansible.builtin.include: configuration/directories.yaml

- name: Ensure Deluge users
  ansible.builtin.include: configuration/users.yaml

- name: Ensure Deluge configuration files
  ansible.builtin.include: configuration/files.yaml